# Ecosoft - Backend v3

Implements 'Alternativa 1' for role requests: solicitud_rol and rol_solicitado in usuarios.
New users are created as 'Cliente' by default.

SQL migration and endpoint list in the file.
